export interface Product{
    capacidad: number; 
    material:string; 
    articulo:string;
    imperm:number, 
    image:string; //es una url
    stock:number;
    clearance:boolean;
    precio:number;
    cantidad:number;
}

